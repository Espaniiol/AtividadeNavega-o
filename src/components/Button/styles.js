import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button:{
        height: 48,
        borderRadius: 10,
        width: 175,
        marginBottom: 50,
        justifyContent:"center",
        borderWidth: 1,
    },
    textButton:{
        color: "#FFFFFF",
        fontSize: 25,
        fontWeight: "bold",
        textAlign: "center"
        
    }
})